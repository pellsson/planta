html
	head
		title Plantaor
	body
		h1 Wasabi
		each cam in cameras
			h3= cam.name
			div
				each vid in cam.videos
					span(style="padding: 8px;")
						a(href=vid.path)= vid.title
			each img in cam.images
				a(href=img)
					img(src=img alt=img width=128 style="padding: 8px")
